Status: done
Created: 2026-07-21
Updated: 2026-07-21

# Prepare release 0.6.0

## Scope

Prepare version 0.6.0 from the current validated main work: the local-token weekly trend and the Dock allowance refresh fix. Update release metadata, validate the Store upload, commit the scoped changes, and push them to `origin/main`.

## Non-goals

- Do not submit a package to Partner Center.
- Do not create a Git tag or GitHub Release.
- Do not change Store identity, publisher, signing configuration, or tracked development manifest version.

## Acceptance criteria

- `CodexUsageDock.csproj` and the version assertion report `0.6.0`.
- `CHANGELOG.md` has a dated `0.6.0` section containing the unreleased user-facing entries and commit links.
- Native ARM64 tests and release package validation pass.
- `scripts/build-release.ps1` produces the versioned Store upload and checksum.
- Only inspected release and Dock-fix files are committed and `origin/main` is verified at the resulting commit.

## References

- `.10x/tickets/2026-07-20-dock-allowance-refresh.md`
- `DEVELOPMENT.md` release workflow
- `scripts/build-release.ps1`

## Assumptions

- User-ratified: release version is `0.6.0`.
- User-ratified: prepare and publish source to `origin/main`; Store submission, tag, and GitHub Release remain out of scope.

## Journal

- 2026-07-21: User approved version `0.6.0` after the proposed SemVer minor increment for the local-token trend feature plus Dock fix.
- 2026-07-21: Full native ARM64 tests passed (113/113). The official release builder produced and validated `CodexUsageDock-0.6.0.msixupload` and `SHA256SUMS.txt` containing x64 and ARM64 packages.

## Evidence

- `CodexUsageDock.csproj` and the version assertion both use `0.6.0`.
- `scripts/build-release.ps1` completed and produced the Store upload plus SHA-256 checksum under `artifacts/store`.
- The source manifest remained unchanged by the build; the builder supplied the Store package version only in its disposable build-local manifest.

## Review

- Verdict: pass
- Scope review: the release contains only the allowance-refresh regression fix, version metadata, changelog, and the associated task records. Generated artifacts remain untracked.
- Residual risk: the Command Palette host must still be manually reloaded to visually verify the Dock after the provider-level invalidation fix.

## Retrospective

- The official builder needs time beyond its first x64 package output to finish the ARM64 package and bundle. Release readiness is established only when both the `.msixupload` and checksum are present.

## Blockers

- None.
